query FlaggedSymbols($pub: String!, $watchlistId: ID!) {
  watchlist(pub: $pub, watchlistId: $watchlistId) {
    id
    name
    lastModifiedDateUtc
    description
    items {
      symbol
      key
      dowJonesKey
    }
  }
}
